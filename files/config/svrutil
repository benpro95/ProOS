#!/bin/bash
### Run PVE script in screen terminal

SCRNVAR=$(echo $RANDOM)

if [ "$1" = "" ] ; then
  /usr/bin/screen -dmS $SCRNVAR /usr/bin/svrutil.bin $1
  sleep 0.25
  screen -rS $SCRNVAR
else
  /usr/bin/svrutil.bin $1
fi

exit